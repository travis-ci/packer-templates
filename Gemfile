# vim:filetype=ruby
source 'https://rubygems.org'

ruby '2.2.3' if ENV.key?('DYNO')

gem 'faraday'
gem 'pry', group: %w(test development).map(&:to_sym)
gem 'rake'
gem 'rspec'
gem 'rubocop'
gem 'serverspec'
