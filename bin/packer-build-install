#!/usr/bin/env bash

set -o errexit

cd $(dirname $(dirname ${BASH_SOURCE[0]}))

mkdir -p tmp/
echo "${GCE_ACCOUNT_FILE_B64_BZ2}" | \
  base64 -d | \
  bunzip2 > tmp/gce.json

if [[ "$(packer version 2>/dev/null)" != "Packer v0.10.1" ]] ; then
  make install-packer
fi
