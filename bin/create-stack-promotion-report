#!/usr/bin/env ruby
libdir = File.expand_path('../../lib', __FILE__)
$LOAD_PATH.unshift(libdir) unless $LOAD_PATH.include?(libdir)

require 'stack_promotion_reporter'
StackPromotionReporter.report! if $PROGRAM_NAME == __FILE__
