#!/bin/bash

DEST="packer@image-builder.macstadium-us-se-1.travisci.net"

# Sync our current packer-templates to the build machine
rsync -avz --exclude='.git' --exclude='.env*' ./ $DEST:/home/packer/packer-templates

# Start the build
ssh -t $DEST bin/build-macos make ci-macos "$@"
